{
  "type": "object",
  "required": ["container_name"],
  "properties": {
    "container_name": {
      "type": "string",
      "title": "Container Name",
      "default": "vaultwarden"
    },
    "data_path": {
      "type": "string",
      "title": "Data Path Inside Container",
      "default": "/data"
    },
    "health_url": {
      "type": "string",
      "title": "Health URL (optional)",
      "description": "Unauthenticated /alive URL used after restore when the container has no Docker healthcheck. The container IP is auto-detected when omitted."
    }
  }
}
