LOG_LEVEL=INFO
# SQLAlchemy echo options:
#   0 / false / off  -> no SQL statements (default)
#   1 / true / on    -> basic SQL statements
#   debug            -> SQL statements with bound parameters
LOG_SQL_ECHO=0
TZ=Asia/Singapore
BACKUP_ROOT_HOST=/tmp/backups
DB_FOLDER=./db